# Copy to .streamlit/secrets.toml for local Streamlit runs,
# or paste into Streamlit Cloud -> App -> Settings -> Secrets.

OPENAI_API_KEY = "sk-..."
AGENTIC_REQUIRE_LIVE_RAG = "true"

# Optional private data logging repo
GITHUB_TOKEN = "ghp_..."
GITHUB_REPO = "owner/hiring-study-data-private"
